apiVersion: v1
kind: Service
metadata:
  name: dummy-deployment
  namespace: konflux-dummy-deploy
  labels:
    app: dummy-deployment
spec:
  selector:
    app: dummy-deployment
  ports:
    - port: 80
      targetPort: 8080
